$boxes = $('<%= j render(@pins) %>')

$('#pins').append( $boxes ).imagesLoaded( function(){
	$('#pins').msnry.reloadItems();
});
<% if @pins.next_page %>
	$('.pagination').replaceWith('<%= j will_paginate(@pins) %>');
<% else %>
	$('.pagination').remove();
<% end %>